{
  "name": "neko-nap",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -b",
    "validate-levels": "vitest run tests/levels.test.ts",
    "analyze-levels": "node --import tsx scripts/analyze-levels.ts"
  },
  "dependencies": {
    "react": "latest",
    "react-dom": "latest"
  },
  "devDependencies": {
    "@types/react": "latest",
    "@types/react-dom": "latest",
    "@vitejs/plugin-react": "latest",
    "tsx": "^4.23.12",
    "typescript": "latest",
    "vite": "latest",
    "vitest": "latest"
  }
}
